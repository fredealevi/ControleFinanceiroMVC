@model Controle_Financeiro.Models.Receita

@{
    ViewData["Title"] = "Deletar Receita";
}

<h2 style="color: red;">Deseja Realmente Deletar a Receita?</h2>

</hr>

<div>
    <dl>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(x => x.Valor)
        </dt>
        <dt class="col-sm-10">
            @Html.DisplayFor(x => x.Valor)
        </dt>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(x => x.Tipo)
        </dt>
        <dt class="col-sm-10">
            @Html.DisplayFor(x => x.Tipo)
        </dt>
    </dl>
</div>

<form asp-action="Deletar">
    <input type="submit" value="Deletar" class="btn btn-danger"/>
</form>
<br>
<a asp-action="Index">Voltar</a>